from django.apps import AppConfig


class ParcelasConfig(AppConfig):
    """Configuración de la aplicación de parcelas."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'parcelas'
    verbose_name = 'Parcelas'
